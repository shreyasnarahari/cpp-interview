# Real Interview Questions: Strings & Formatting

1. How does Small String Optimization (SSO) work, and how do you measure its threshold?
2. What are the common lifetime traps when passing `std::string_view`?
3. Why is `std::format` faster than `std::ostringstream` and `printf`?
4. How do you implement a custom `std::formatter` for a user-defined class?
