# Real Interview Questions: Build Systems & Toolchains

1. What is the difference between `PUBLIC`, `PRIVATE`, and `INTERFACE` target visibility in CMake?
2. How does Link-Time Optimization (LTO / IPO) enable cross-translation-unit inlining?
3. How does AddressSanitizer (ASan) detect use-after-free and buffer overflows at runtime?
4. What is the difference between static libraries (`.a`/`.lib`) and dynamic shared libraries (`.so`/`.dll`)?
